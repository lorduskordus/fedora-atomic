name: fedora-base-gnome-nvidia
description: Custom Fedora GNOME image based on uBlue, built with BlueBuild. (Base, with NVIDIA support)

base-image: ghcr.io/lorduskordus/fedora-core-gnome-nvidia
image-version: coreos
alt-tags:
  - coreos

modules:

  # Common
  - from-file: common/config.yml
  
  # GNOME
  - from-file: gnome/config.yml
