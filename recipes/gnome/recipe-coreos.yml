name: fedora-base-gnome
description: Custom Fedora GNOME image based on uBlue, built with BlueBuild. (Base)

base-image: ghcr.io/lorduskordus/fedora-core-gnome
image-version: coreos
alt-tags:
  - coreos

modules:

  # Common
  - from-file: common/config.yml
  
  # GNOME
  - from-file: gnome/config.yml
