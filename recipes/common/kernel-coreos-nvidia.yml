modules:

  - from-file: common/kernel-coreos.yml

  - type: containerfile
    snippets:
      - COPY --from=ghcr.io/ublue-os/akmods-nvidia:coreos-stable-41 /rpms /tmp/akmods-rpms

  - type: script
    scripts:
      - common/nvidia-install.sh
