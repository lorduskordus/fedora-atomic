name: fedora-base-kde-nvidia
description: Custom Fedora KDE image based on uBlue, built with BlueBuild. (Base, with NVIDIA support)

base-image: ghcr.io/ublue-os/kinoite-main
image-version: 41
alt-tags:
  - coreos

modules:

  # Kernel
  - from-file: kernel/coreos.yml

  # Common
  - from-file: common/config.yml

  # KDE
  - from-file: kde/config.yml
