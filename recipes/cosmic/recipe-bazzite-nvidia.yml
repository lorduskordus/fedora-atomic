name: fedora-base-cosmic-nvidia
description: Custom Fedora COSMIC image based on uBlue, built with BlueBuild. (Base, with NVIDIA support)

base-image: ghcr.io/lorduskordus/fedora-core-cosmic-nvidia
image-version: bazzite
alt-tags:
  - bazzite

modules:

  # Common
  - from-file: common/config.yml
  
  # COSMIC
  - from-file: cosmic/config.yml
