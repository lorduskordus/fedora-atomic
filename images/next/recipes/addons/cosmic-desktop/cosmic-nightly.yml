type: dnf
repos:
  cleanup: true
  copr:
    - ryanabx/cosmic-epoch
install:
  packages:
    - cosmic-desktop

    # Missing dependencies
    - gnome-keyring-pam # Pulls 'gnome-keyring'
    - gvfs-mtp
    - gvfs-afc
    - gvfs-gphoto2
