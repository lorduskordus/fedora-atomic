# Explanation

- Scripts under the 'container_scripts' folder are supposed to run inside the container.
- The file 'fish_prompt.fish' is copied to '/etc/fish/functions', otherwise it doesn't work in the container.
